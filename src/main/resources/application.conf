thingy {

  seed-network = "./src/main/resources/seed.json"
  seed-subnet = "./src/main/resources/subnet-seed.json"
	weight-range = 5
  population-size = 100
  generations = 25
  species-starting-threshold = 2.0
  species-expansion-factor = 0.1
  crossover-rate = 0.8
  mutation-rate = 0.75
  jiggle-over-reset=0.9
  mutation-weights {
    network-connection = 0.5
    sub-network-connection = 0.5
  }

}


akka {
  loglevel = "DEBUG"
   # event-handlers = ["akka.event.slf4j.Slf4jEventHandler"]
  loggers = ["akka.event.slf4j.Slf4jLogger"]
  actor {
   debug {
    receive= on
   	fsm = on
   }
  }
}


kamon {

  metric {
	  tick-interval = 30 second
	  filters {
		akka-actor {
		  includes = [ "default/user/**" ]
        excludes = []
      }
      trace {
        includes = [ "**" ]
        excludes = []
      }
      akka-dispatcher {
        includes = [ "akka.actor.default-dispatcher" ]
        excludes = []
      }
      akka-router {
        includes = ["**"]
        excludes = []
      }
    }
  }
}
