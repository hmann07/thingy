# Routes
# This file defines all application routes (Higher priority routes first)
# ~~~~

# An example controller showing a sample home page
GET     /                           controllers.HomeController.index

# the net viewer
GET     /netviewer                  controllers.NetViewerController.index

GET     /mongolookup/:speciesId		controllers.MongoLookupController.findBySpecies(speciesId)

GET     /getSpeciesByGeneration/:generationId		controllers.MongoLookupController.getSpeciesByGeneration(generationId)


GET /getNetByGenerationAndSpecies/:generationId/:speciesId controllers.MongoLookupController.findBySpeciesandGen(generationId, speciesId)

GET     /generations 				controllers.MongoLookupController.getGenerations

GET     /start 						controllers.StartEvolveController.start

# An example controller showing how to use dependency injection
GET     /count                      controllers.CountController.count
# An example controller showing how to write asynchronous code
GET     /message                    controllers.AsyncController.message

# Map static resources from the /public folder to the /assets URL path
GET     /assets/*file               controllers.Assets.versioned(file)

->     /webjars                	webjars.Routes
